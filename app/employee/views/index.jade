extends ../../../views/layout

block content
  p: b list of drivers
    a(href='/employee/new', class='button-link') add new employee
  hr
  ul
    for driver, i in drivers
      li: a(href="/employee/#{driver.id}") #{driver.id}. #{driver.name} [ #{driver.bas} ]

  hr
  p: b end of drivers
